info:
  name: Redirect to URL
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/:short_code"
  params:
    - name: short_code
      value: PzywlOYlG
      type: path
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://0.0.0.0:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
