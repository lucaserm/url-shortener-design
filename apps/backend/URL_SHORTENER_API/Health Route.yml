info:
  name: Health Route
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/health"
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: http://0.0.0.0:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
